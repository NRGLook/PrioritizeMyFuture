from src.User import User

if __name__ == "__main__":
    user = User()



























    """
    # user registration
    user = User()
    # adding a task
    user = RegisteredUser()
    task = Task()
    user.add_task(task)
    # removing a task
    user = RegisteredUser()
    task = Task()
    user.tasks = [task]
    user.remove_task(task)
    # updating a task
    user = RegisteredUser()
    task1 = Task()
    task2 = Task()
    user.tasks = [task1]
    user.update_task(task1, task2)
    # burning tasks for the day
    user = RegisteredUser()
    task1 = Task()
    task2 = Task()
    user.tasks = [task1, task2]
    user.burn_today()
    # transferring tasks to the future
    user = RegisteredUser()
    task1 = Task()
    task2 = Task()
    user.tasks = [task1, task2]
    user.transfer_to_future()
    # setting the bank name and styles
    bank = Bank()
    bank.set_name("My Bank")
    bank.set_styles(["Savings", "Investment"])
    # changing bank styles
    bank = Bank()
    bank.set_styles(["Savings"])
    bank.change_styles(["Investment"])
    # setting the bank volume and yesterday's outcome
    bank = Bank()
    bank.set_volume(1000)
    bank.set_y_o(200)
    # changing yesterday's outcome for the bank
    bank = Bank()
    bank.set_y_o(200)
    bank.change_y_o(50)
    # setting the cost, name, status, and category for a task
    task = Task()
    task.set_cost_name(60)
    task.set_name("Study")
    task.set_status("Incomplete")
    task.set_category("Education")
    """
